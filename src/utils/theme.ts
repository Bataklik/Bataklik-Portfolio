import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#5c6bc0",
      contrastText: "#fff",
    },
    secondary: {
      main: "#d32f2f",
      contrastText: "#fff",
    },
  },
});
